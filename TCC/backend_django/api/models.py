# from django.db import models

# Models não criados pois o banco de dados já existe
# As tabelas (usuario, evento, sessoes, aluno_evento) já estão em database_beta_tcc
# Este é um projeto que usa uma banco de dados legacy

# Se quiser usar models Django no futuro, crie-os opcionalmente aqui
